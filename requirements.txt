google-generativeai==0.8.4 # Ensure proper version compatibility
pillow==11.1.0          # For PIL.Image
opencv-python==4.11.0.86 # For cv2 (or a version that is compatible)
numpy==1.26.4        # Ensure compatibility